请你首先阅读工作区代码,尤其是数据库与模型定义部分，充分了解代码层级架构、错误处理与回调、业务基本架构与逻辑，然后根据以下要求进行生成。

总目标：你需要实现 utils-venuePermission 模块的内容

说明：

- 场地权限共有四种：reserve（可预约）、approval（可审批）、manage（可管理）、edit（可编辑），你可参考 venue_accesses.sql 中的数据表定义。

- internal\utils\venuePermission 中实现 refreshVenueAccessCache 函数，从数据库载入权限数据到内存（使用map[int]VenueAccess）

- 在model中实现作为VenueAccess模型的对应的四个权限的检验方法，输入venue_id，输出bool

- repo 层对于 venueAccess 只实现了一些通用功能，你可以根据需要修改或追加需要的函数

- internal\middleware\venuePermission.go 中实现对权限校验的包装

最终效果：

controller 可以直接使用 CheckVenuePermission(venuePermission.Reserve) 来控制API访问，参考已实现的 systemPermission
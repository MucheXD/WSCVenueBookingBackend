请你首先阅读工作区代码，尤其是数据库与模型定义部分，充分了解代码层级架构、模型相互转换、错误处理与回调、业务基本架构与逻辑，然后根据以下要求进行生成。

## 总体要求

### 总目标

工作区代码为一个场地管理系统的 go 后端，请你现在实现场地管理的增删改查部分，包括与前端对接的 Controller 层及其 DTO 模型、业务层逻辑及其 Model 模型、数据访问层及其 Entity 模型。我会详细告知你各部分模型的具体要求与逻辑框架，同时请你参考已有的 sql 文件与 Model 文件。

## 业务架构

场地的权限管理逻辑为：单独一表记录权限组具体权限，每个用户分配一个权限组，决定用户可否进行场地有关操作。

场地的位置分类逻辑为：各场地有其所属的 building（楼区），通过 location_building_id 字段记录；各楼区有其所属的 campus（校区），通过 location_campus_id 记录（请参阅venue_buildings.sql）；前端会首先请求楼区列表，包含楼区的显示名称等，而后与后端的数据交换全部通过id进行。

## 详细要求

### 新建场地

API：PUT api/venue

API 功能：创建一个新的场地

API 权限：需要具有 CreateNewVenue 系统权限

API 链路：接受前端表单-svc层校验合法性-数据库存储

API 表单：
- name string
- building_id int
- type_id int (可选)
- description string (可选)
- cover_image_token string (可选)
- images_token []string (可选)
- capacity (可选)

API 成功返回表单：
- venue_id int

### 修改场地

API：PUT api/venue/{venue_id}

API 功能：修改一个已有场地的场地信息

API 权限：需要具有对应场地的 Edit 权限或者 AllVenueEdit 系统权限

API 链路：接受前端表单-svc层校验合法性-svc层更新既有模型-数据库更新

API 表单：等同“新建场地”接口，但所有字段均可选，只更新前端回传的字段

### 删除场地

API：DELETE api/venue/{venue_id}

API 功能：删除一个已有场地（请使用软删除）

API 权限：需要具有对应场地的 Edit 权限或者 AllVenueEdit 系统权限

API 链路：接受前端表单-svc合法性判断-数据库删除操作

API 表单：无

### 列出场地

API：GET /api/venue

API 功能：列出用户可访问的所有场地

API 权限：需要具有对应场地四种权限中的任意权限，或者对应系统四种权限中的任意权限

API 链路：接受前端表单-svc层进行查询建构-数据库查询

API 表单：
- b []int (Query/可选) :筛选位于特定楼区（building_id）的场地
- t []int (Query/可选) :筛选具有特定标签（type_id）的场地
- p []string (Query/可选) :筛选用户具有特定权限的场地，值可以为R/A/M/E的任意组合，对应四种场地权限
- s string (Query/可选) :搜索关键词
- o int (Query/可选) :分页偏移（offset）
- n int (Query/可选) :单页最大（pagesize），无此项默认 12

API 成功返回表单：
- []object
    - venue_id int
    - name int
    - building_id int
    - type_id int
    - description_text string
    - cover_image_token string
    - permission []string
    - attachments []object
        - type string
        - index int
        - file_token string
    - timetable []object
        - start string
        - end string
        - status string

业务逻辑：

前端的“可预约场地”页面与“场地审批”页面均调用此接口，区别仅仅为 p 筛选器字段。
例如（所有ID均为虚构）：
- 查询“教学楼和体育馆所有可预约的会议室场地”的调用可能为：GET /api/venue?b=13,17&t=7&p=R
- 查询“关键词为‘物理’的所有教学楼我可审批的实验室场地”的调用可能为：GET /api/venue?b=13&t=8&p=A&s="物理"

attachments 字段从 attachments 表查询 biz_type=“venue” biz_id=<venue_id> 的条目。

timetable 字段从 venue_timeslots 表查询，为未来的场地占用情况。距今超过7日的占用信息不予返回。最多返回32条占用信息。

分页偏移（offset）指定的数字为“条目”而非“页面”，即“o=12”时才返回下一页（默认情况下）。

由于有分页设计，因此不应该取出符合条件的场地再进行权限筛选，而是应该构建联表查询，提供 vagid 先联表查询 venue_accesses 中出现过的 venue_id（在对应vagid条目出现过即代表至少有其中一种权限），再 JOIN 到 venues 表实现一次查询。生成的 GORM SQL 调用应在注释中简要说明，以便我审核。

### 列出楼区与校区信息

API：GET api/venue/locations

API 功能：提供楼区与校区信息，以便前端进行显示

API 权限：需要具有楼区包含的任意场地的场地四种权限中的任意权限，或者对应系统四种权限中的任意权限

API 链路：接受前端表单-查询用户VAGID对应可用场地-查询用户可用场地对应的楼区-查询楼区信息-查询校区信息并返回

API 表单：无

API 成功返回表单：
- []object
    - building_id int
    - building_name string
    - location_campus_id int
- []object
    - campus_id int
    - campus_name string

业务逻辑：

登录的用户可直接从上下文取得 VAGID，venueAccessCache可以找到缓存的权限表，去 venues 表查询可得对应的楼区ID列表，在 venue_buildings 查询获得楼区信息并返回为第一个对象列表，对所有提及的 location_campus_id 在 venue_campuses 查询获得校区信息并返回作为第二个对象列表。

上面的仅为可行的思路，为了避免三次SQL查询，在实际编码时你应该使用JOIN方法来节省查询次数。请使用 GORM 的 Joins 或 Preload 预加载等功能（根据需要自行判断），以解决 N+1 查询问题。生成的 SQL 应在注释中简要说明，以便我审核。


=== Code Review ===

我完成了对你上一次编辑的审阅，在需要修改的地方使用了"cmt"标签的注释，请你仔细理解修改需求，组织修改思路，然后重构代码。

自上次编辑后，我提出了 19 处具有"cmt"标签的注释，请你逐个处理。问题主要集中在：

1. 你在 controller 层正确处理了前端到后端的数据转换，但却在 service 层进行后端到前端的数据转换。请将后端到前端的数据转换转移到 controller 层中。如果一个返回包体由多个“部分”组成，请在 controller 层完成分步请求与拼装，而不是在 service 层。

2. 你的部分对权限的查询有冗余，请查阅 utils-venuePermission 的代码，这里已经实现了权限的内存缓存机制与权限检查。

3. 你的“查询是否存在”的操作需要修改 repo 层来完成（增加一个方法），而不是通过“获取-判断有无错误”来完成。同时，service 层不应该依赖 gorm。

完成编辑后，请保留 "cmt" 标签，但将后面的注释改为你修改的内容以便我核验。